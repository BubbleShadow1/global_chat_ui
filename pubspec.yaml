name: global_chat
description: Global Chat is a backend-agnostic, highly configurable Flutter chat UI toolkit.
version: 0.2.0
homepage: https://pub.dev/packages/global_chat
topics:
  - chat
  - messaging
  - flutter
  - ui

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
